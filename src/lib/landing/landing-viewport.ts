/** Matches portal desktop breakpoint (`globals.css` desktop-only gate). */
export const LANDING_DESKTOP_MIN_WIDTH_PX = 1024;

export const LANDING_MOBILE_TABLET_MEDIA_QUERY = `(max-width: ${LANDING_DESKTOP_MIN_WIDTH_PX - 1}px)`;
